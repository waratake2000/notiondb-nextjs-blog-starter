interface HeaderProps {
  children: String;
  subText: String;
}

interface HeaderTitleProps {
  children: String;
  subText: String;
}

interface TagProps {
  label: string;
  url?: string | null;
}

interface TitleTextProps {
  children: string;
}

interface FooterProps {
  
}